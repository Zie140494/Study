<Window x:Class="Inventory.AuthentificationForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Inventory"
        mc:Ignorable="d"
        Height="180" Width="300" Title="Вход в систему">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Label HorizontalAlignment="Center" VerticalAlignment="Center">Имя пользователя:</Label>
        <Label Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">Пароль:</Label>
        <TextBox x:Name="NameTb" Grid.Column="1" Margin="10" ></TextBox>
        <TextBox x:Name="PassTb" Grid.Column="1" Grid.Row="1" Margin="10"></TextBox>
        <Button Grid.Row="2" Margin="10" Click="OK_Click">OK</Button>
        <Button Grid.Column="1" Grid.Row="2" Margin="10" Click="Cancel_Click">Отмена</Button>
    </Grid>
</Window>
